import React from 'react';

import {Meta} from '@storybook/react';

import {Button} from './SettingsButton';

export default {
  component: Button,
  title: 'Components/SettingsButton',
} as Meta;

// export const SettingsButton: React.VFC<{}> = () => <Button>Button</Button>;
