import React from 'react';

import {Meta} from '@storybook/react';

import {Button} from './SideBarMenu';

export default {
  component: Button,
  title: 'Components/SideBar',
} as Meta;

// export const SideBar: React.VFC<{}> = () => <Button>Button</Button>;
