import React from 'react';

import {Meta} from '@storybook/react';

import {Button} from './MainButton';

export default {
  component: Button,
  title: 'Components/MainButton',
} as Meta;

// export const Main: React.VFC<{}> = () => <Button main>Button</Button>;
